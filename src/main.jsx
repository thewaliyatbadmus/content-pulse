import React from 'react';
import ReactDOM from 'react-dom/client';
import ContentPulse from './ContentPulse.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ContentPulse />
  </React.StrictMode>
);
